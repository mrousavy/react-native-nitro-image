///
/// HybridNitroAnimatedImageViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroAnimatedImageViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroAnimatedImageViewSpecSwift.hpp"
#import "NitroAnimatedImage-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::animated::image;
using namespace margelo::nitro::animated::image::views;

/**
 * Represents the React Native View holder for the Nitro "NitroAnimatedImageView" HybridView.
 */
@interface HybridNitroAnimatedImageViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroAnimatedImageViewComponent {
  std::shared_ptr<HybridNitroAnimatedImageViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroAnimatedImageViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroAnimatedImageViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridNitroAnimatedImageViewSpec> hybridView = NitroAnimatedImage::NitroAnimatedImageAutolinking::createNitroAnimatedImageView();
    _hybridView = std::dynamic_pointer_cast<HybridNitroAnimatedImageViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroAnimatedImage::HybridNitroAnimatedImageViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridNitroAnimatedImageViewProps const>(props);
  auto& newViewProps = const_cast<HybridNitroAnimatedImageViewProps&>(newViewPropsConst);
  NitroAnimatedImage::HybridNitroAnimatedImageViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // image: optional
  if (newViewProps.image.isDirty) {
    swiftPart.setImage(newViewProps.image.value);
    newViewProps.image.isDirty = false;
  }
  // resizeMode: optional
  if (newViewProps.resizeMode.isDirty) {
    swiftPart.setResizeMode(newViewProps.resizeMode.value);
    newViewProps.resizeMode.isDirty = false;
  }
  // recyclingKey: optional
  if (newViewProps.recyclingKey.isDirty) {
    swiftPart.setRecyclingKey(newViewProps.recyclingKey.value);
    newViewProps.recyclingKey.isDirty = false;
  }
  // autoplay: optional
  if (newViewProps.autoplay.isDirty) {
    swiftPart.setAutoplay(newViewProps.autoplay.value);
    newViewProps.autoplay.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroAnimatedImage::NitroAnimatedImageAutolinking::isNitroAnimatedImageViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroAnimatedImage::HybridNitroAnimatedImageViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

@end
