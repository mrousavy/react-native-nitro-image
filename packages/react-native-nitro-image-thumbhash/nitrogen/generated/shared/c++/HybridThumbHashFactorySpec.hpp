///
/// HybridThumbHashFactorySpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `HybridImageSpec` to properly resolve imports.
namespace margelo::nitro::image { class HybridImageSpec; }

#include <NitroModules/ArrayBuffer.hpp>
#include <memory>
#include <NitroImage/HybridImageSpec.hpp>
#include <NitroModules/Promise.hpp>
#include <string>

namespace margelo::nitro::image::thumbhash {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ThumbHashFactory`
   * Inherit this class to create instances of `HybridThumbHashFactorySpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridThumbHashFactory: public HybridThumbHashFactorySpec {
   * public:
   *   HybridThumbHashFactory(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridThumbHashFactorySpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridThumbHashFactorySpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridThumbHashFactorySpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<ArrayBuffer> encode(const std::shared_ptr<margelo::nitro::image::HybridImageSpec>& image) = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<ArrayBuffer>>> encodeAsync(const std::shared_ptr<margelo::nitro::image::HybridImageSpec>& image) = 0;
      virtual std::shared_ptr<margelo::nitro::image::HybridImageSpec> decode(const std::shared_ptr<ArrayBuffer>& thumbhash) = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<margelo::nitro::image::HybridImageSpec>>> decodeAsync(const std::shared_ptr<ArrayBuffer>& thumbhash) = 0;
      virtual std::string toBase64String(const std::shared_ptr<ArrayBuffer>& thumbhash) = 0;
      virtual std::shared_ptr<ArrayBuffer> fromBase64String(const std::string& thumbhashBase64) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ThumbHashFactory";
  };

} // namespace margelo::nitro::image::thumbhash
